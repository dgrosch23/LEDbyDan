{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "VacationMedia": {
    "_comment": "Local development points at the bundled SampleVacations folder instead of a real network share, so 'dotnet run' works out of the box.",
    "RootPath": "SampleVacations",
    "CacheDurationSeconds": 5
  }
}
